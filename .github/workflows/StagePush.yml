name: StagePush
run-name: "StagePush ${{ github.run_id }}/${{ github.run_number }}/${{ github.run_attempt }} (${{ github.action }}) by @${{ github.actor }}"

on:
  push:
    branches:
      - 'stage/*'
    paths:
      - 'i18n/*'

jobs:
  wrapper:
    uses: chrsmj/such-action/.github/workflows/SharedStagePush.yml@main
