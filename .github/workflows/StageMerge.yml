name: StageMerge
run-name: "StageMerge ${{ github.run_id }}/${{ github.run_number }}/${{ github.run_attempt }} (${{ github.action }}) by @${{ github.actor }}"

on:
  workflow_run:
    workflows:
      - 'StagePush'
    branches:
      - 'stage/*'
    types:
      - 'completed'

jobs:
  wrapper:
    uses: chrsmj/such-action/.github/workflows/SharedStageMerge.yml@main
