name: "Stage Merge"

on:
  workflow_run:
    workflows:
      - 'Stage Push'
    branches:
      - 'stage/*'
    types:
      - 'completed'

jobs:
  first:
    uses: chrsmj/such-action/.github/workflows/SharedStageMerge.yml@main
